{
  "id": "${UUID}",
  "arch": "aarch64",
  "allow_pxe": true,
  "hostname": "${HOSTNAME}",
  "ip_addresses": [
    {
      "address": "${IP}",
      "netmask": "${MASK}",
      "address_family": 4
    }
  ],
  "network_ports": [
    {
      "data": {
        "mac": "${MAC}"
      },
      "name": "eth0",
      "type": "data"
    }
  ],
  "allow_workflow": true
}
